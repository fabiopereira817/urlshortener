# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /:shortUrl             controllers.UrlShortenerController.getOriginalUrl(shortUrl: String)
POST    /                      controllers.UrlShortenerController.saveUrl()
